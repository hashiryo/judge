# 自前実験: GF(2^64)* の離散対数 log_g(x)。
# g = 2 (= 多項式基底の x) は P(x) = x^64+x^4+x^3+x+1 の下で原始元 (= 位数 2^64-1)。
# 答え k は [0, 2^64 - 2] の範囲で一意 (g が原始元なので)。
# が、実装によっては別の同値表現 (k mod 0) を返す可能性もあるため、checker 経由で
# g^k_user == x を確認する special judge 形式にしておく (より頑健)。
url = ""
tle = 10
mle = 256
callgrind_case = "random_medium_00"
